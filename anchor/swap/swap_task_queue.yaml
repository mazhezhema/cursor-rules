swap_task_queue:
  desc: 定义从上传到任务执行、状态检查与回调的消息队列结构。
  fields:
    callback_hooks: 后处理回调与前端通知策略
    queue_system: 使用的队列系统（如 Redis、RQ、RabbitMQ）
    task_lifecycle: 任务生命周期状态流转
    timeout_strategy: 超时/断线任务处理机制
  tag:
  - swap
  - task
  - queue
  title: 换脸任务队列与回调流程
