performance:
  animation_limit:
    description: 减少动画层级与持续时间，优先加载速度。
    implementation_hint: 默认关闭复杂过渡动画，使用轻量Fade或缩放替代。
  image_loading:
    description: 异步加载图片并进行懒加载。
    implementation_hint: 前端需封装统一图片加载组件，处理loading与fallback图。
