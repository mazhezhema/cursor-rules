# Anchor: auth_identity_conflict
description: >
  描述 OAuth 登录时出现身份冲突的处理方式，如邮箱重复、账号绑定冲突等。
spec:
  - 冲突识别：判断用户绑定过的邮箱、手机号等是否已被其他 OAuth 提供方绑定。
  - 冲突提示：提供统一弹窗交互引导用户选择合并或重新绑定。
  - 数据一致性策略：保持原账号数据不被覆盖，提示用户处理方案。