id: test_tdd_principle
title: TDD 开发原则约定
description: |
  明确所有新功能、bug 修复必须遵循测试驱动开发流程，先写 test case 再开发逻辑。
  所有 PR 都需包含相应测试。
structure:
  - rule:
      - 每个 feature PR 必须包含至少 1 个自动化测试
      - 所有测试文件应放入 test 目录并通过 CI 检查
  - enforcement:
      - CI 不通过则禁止 merge
      - 每月评估测试覆盖率变化，纳入绩效指标