anchor: backend_version_negotiation
title: 后端版本协商机制
description: 定义客户端与服务端之间的API版本协商与兼容策略，确保新旧客户端都能正确通信。
tags: [versioning, api, compatibility]
spec:
  - 支持客户端通过Header或Query参数传递版本号
  - 后端根据版本号返回不同的处理逻辑
  - 保证主版本兼容性，小版本兼容性