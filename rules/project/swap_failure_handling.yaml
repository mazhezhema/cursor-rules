swap_failure_handling:
  desc: 针对输入图像无脸、Roop失败、任务中断等异常情况的兜底方案。
  fields:
    error_types: 常见错误类型与对应识别方式
    fallback_template: 失败时使用的默认图源模板
    retry_policy: 是否支持重新提交机制
    user_feedback: 错误提示文案与用户引导
  tag:
  - swap
  - fallback
  - error
  title: 换脸失败处理机制
