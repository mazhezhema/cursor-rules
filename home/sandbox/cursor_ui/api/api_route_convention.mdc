接口路由命名与实现规范：

目的：保持 API 命名统一、语义明确，方便前端集成与后期扩展。

规范要点：
1. 所有接口以 `/v1/` 为前缀，保留版本控制能力。
2. GET 方法用于查询，POST 用于提交上传。
3. 上传图像采用 multipart/form-data，字段为 `image`。
4. 用户鉴权接口需标记 `auth_required: true`，前端统一加上 token 头。
5. 错误响应统一封装为 4xx/5xx + json 错误码，Cursor 将自动解析错误并跳转错误提示页。

建议与 page 路由保持命名同步，便于界面与接口联动生成。
