title: 历史记录页面
description: 显示用户上传与生成的历史换脸记录，支持再次换脸、再次分享
layout:
  - app_bar
  - list_recent_faces
  - empty_state_prompt
components:
  - AppBar(title="历史记录")
  - FaceHistoryList(itemCount=10)
  - EmptyView(message="暂无记录，快去换一个脸吧！")